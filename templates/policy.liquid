<section class="content-page content-page--policy">
  <div class="content-page__inner content-page__inner--narrow">
    <header class="content-page__header">
      <h1 class="content-page__title page-title">{{ policy.title }}</h1>
    </header>

    <div class="content-page__content content-page__content--policy">
      {{ policy.body }}
    </div>
  </div>
</section>
